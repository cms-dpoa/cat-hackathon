# version: 0.3.0
# inputs:
#   files:
#     - code/helloworld.py
#     - data/names.txt
#   parameters:
#     helloworld: code/helloworld.py
#     inputfile: data/names.txt
#     outputfile: results/greetings.txt
#     sleeptime: 0
# workflow:
#   type: serial
#   specification:
#     steps:
#       - environment: 'python:2.7-slim'
#         commands:
#           - python "${helloworld}"
#               --inputfile "${inputfile}"
#               --outputfile "${outputfile}"
#               --sleeptime ${sleeptime}
# outputs:
#   files:
#    - results/greetings.txt

version: 0.6.0
inputs:
  files:
    - data/names.txt
  parameters:
    outputfile: outputfile.txt
workflow:
  type: serial
  specification:
    steps:
      - environment: 'riga/py-sci'
        commands:
          - mkdir results/
          - echo "Hello, World!" >> results/${outputfile}
outputs:
  files:
   - results/outputfile.txt
